fix: критические исправления импортов и моделей

## Исправлено

### 1. ImportError в pairs.py
- Добавлены недостающие модели в `backend/api/models.py`:
  - Pair, PairDetail, OHLCV
  - Signal (расширенная версия)
  - MonthlyStats, PairStats, CorrelationMatrix
  - EquityPoint (исправлен timestamp на str)
- Добавлены алиасы Side, Timeframe, VolatilityRegime, SignalStatus

### 2. ImportError в telegram/bot.py (конфликт имён)
- Создана новая папка `backend/tg_notifier/` с исправленным bot.py
- Старая `backend/telegram/` теперь содержит редирект на новый модуль
- Исправлен импорт в `backend/live/engine.py`

### 3. Fake data → Real data
- `backend/api/routes/pairs.py` - переписан для работы с БД
- `backend/api/routes/analytics.py` - переписан для работы с БД
- Убраны все `random.*` вызовы
- Данные теперь из TradeModel, PositionModel, SignalModel

## Файлы для замены

```
backend/
├── api/
│   ├── models.py          ← ЗАМЕНИТЬ (добавлены модели)
│   └── routes/
│       ├── pairs.py       ← ЗАМЕНИТЬ (реальные данные)
│       └── analytics.py   ← ЗАМЕНИТЬ (реальные данные)
├── tg_notifier/           ← ДОБАВИТЬ (новая папка)
│   ├── __init__.py
│   └── bot.py
├── telegram/              ← ЗАМЕНИТЬ содержимое
│   ├── __init__.py        ← Редирект
│   └── bot.py             ← Редирект
└── live/
    └── engine.py          ← ЗАМЕНИТЬ (исправлен импорт)
```

## Инструкция

1. Распаковать ZIP в C:\velas\code\Velas-code\ с заменой
2. Проверить: `python -c "from backend.api.models import Pair; print('OK')"`
3. Запустить: START.bat

fix: добавлены ВСЕ недостающие модели в models.py

## Исправлено

### 1. ImportError в routes (models.py)
Добавлены ВСЕ недостающие модели:
- Pair, PairDetail, OHLCV, Signal
- BacktestConfig, BacktestResult
- AllSettings, TradingSettings, TelegramSettings, NotificationSettings
- Preset
- MonthlyStats, PairStats, CorrelationMatrix, EquityPoint
- Алиасы: Side, Timeframe, VolatilityRegime, SignalStatus

### 2. ImportError telegram/bot.py (конфликт имён)
- Создана `backend/tg_notifier/` с исправленным bot.py
- Старая `backend/telegram/` теперь редирект

### 3. Fake data → Real data
- pairs.py и analytics.py работают с реальной БД
- Убраны все random.* вызовы

## Файлы

```
backend/
├── api/
│   ├── models.py          ← ЗАМЕНИТЬ (полный набор моделей)
│   └── routes/
│       ├── pairs.py       ← ЗАМЕНИТЬ
│       └── analytics.py   ← ЗАМЕНИТЬ
├── tg_notifier/           ← ДОБАВИТЬ новую папку
│   ├── __init__.py
│   └── bot.py
├── telegram/              ← ЗАМЕНИТЬ содержимое
│   ├── __init__.py
│   └── bot.py
└── live/
    ├── __init__.py        ← ЗАМЕНИТЬ
    └── engine.py          ← ЗАМЕНИТЬ
```

## Инструкция

1. Распаковать ZIP в C:\velas\code\Velas-code\ с заменой
2. Запустить START.bat
